graph G
    node a, b, c

    a --> c weight integer 3

repeat || G . nodes || - 1 times
    for each x1 <-- x2 with weight w in G
        relax (x1, x2, w)

    for each x1 --> x2 with weight w in G
        if w > 5 then
            return false
        return true